export default function HikeList() {
  //import Spinner, for when hikes are loading from the backend - via a useQuery hook?!

  // todo split into Hikes page and HikeList component
  // const {data,}
  // TODO HOOK TIME BBY

  return <ul>list</ul>;
}
